const buttonSettings = {
    text: "My Button",
    textColor: 'black',
    textFont: 'Arial',
    textSize: 16,
    textWeight: 100,
    backgroundButton: "grey",
}

function changeNameButton(text){
    buttonSettings.text = text;
}